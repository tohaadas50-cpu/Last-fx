# ── TextFX Frontend — Environment Variables ───────────────────────────────────
# Copy to .env and fill secrets locally. Do NOT commit .env.

# Your Railway (or any hosted) backend URL
VITE_API_URL=REPLACE_ME_API_URL
